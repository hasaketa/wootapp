import Reflux from 'reflux';

const ApplyWootActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
   'SubmitWootForm',
   'onSelectProject'
]);


export default ApplyWootActions;
export { ApplyWootActions };
